###########################
# board-query.properties #
###########################
#boardList = select * from board order by reg_date desc
findAll = select * from ( select b.*, (select count(*) from attachment where board_no = b.no) attach_cnt, row_number() over(order by reg_date desc) rnum from board b) b where rnum between ? and ?
findAllBoardAttach = select * from (select row_number() over(order by reg_date desc) rnum, a.* from attachment a)
getTotalContents = select count(*) from board